[project]
name = "lingo"
version = "0.1.0"
description = "WhatsApp live-call echo bot for validating Cloud API Calling integration"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pipecat-ai[silero,webrtc]>=1.8.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "python-dotenv>=1.0.1",
    "aiohttp>=3.10.0",
]

[project.scripts]
lingo = "lingo.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lingo"]

[dependency-groups]
dev = [
    "ruff>=0.12.0",
]

[tool.ruff]
line-length = 100
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
